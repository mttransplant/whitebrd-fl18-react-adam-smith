import React from 'react'

const TopicPills = ({selectedLesson, selectedTopic, selectTopic, addTopic}) =>
    <ul className="nav nav-pills">
        {
            selected.lesson.topics.map((topic, index) =>
                
            )
        }

    </ul>

export default TopicPills